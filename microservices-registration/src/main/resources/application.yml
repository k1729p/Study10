eureka:
  instance:
    hostname: localhost
  client:  # Not a client, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 8761
  
spring:
  thymeleaf:
    enabled: false # Disable Thymeleaf - Eureka dashboard uses FreeMarker 